// TODO: 1. Need to fix the Product page add product, update product, product SKU logic 
// and product variant does not show after refresh

// 2. Need to fix subscription page 





import { createRoot } from "react-dom/client";
import App from "./App.tsx";
import "./index.css";

createRoot(document.getElementById("root")!).render(<App />);
